PORT=${PORT}
WORK_DIR=${WORK_DIR}
LOG_FILE_NAME=${LOG_FILE_NAME}
RDB_FILE_NAME=${RDB_FILE_NAME}
AOF_FILE_NAME=${AOF_FILE_NAME}
PASSWORD=${PASSWORD}
MASTER_NODE_HOST=${MASTER_NODE_HOST}
MASTER_NODE_PORT=${MASTER_NODE_PORT}
MASTER_PASSWORD=${MASTER_PASSWORD}
SENTINEL_PORT=${SENTINEL_PORT}
